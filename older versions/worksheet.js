flyTo = function(){
	
	//make a new timeline

	if(//zigs[zigs.length] is running)
	{
		//make a new div
		//wrap the jigs subject in new div
		//target = new div
	}else{
		//target = jig target
	}

	//animate new timeline (div, according to settings
	//zigs.push()

}